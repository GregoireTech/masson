<!DOCTYPE html>
<html lang="en">

<head>
    <title>Greg est un beau gosse!</title>
    <link rel="stylesheet" href="/css/main.css">
</head>

<body>

    <main>
        <div id='room'>
            <div id='whiteboard'>
                <embed src='https://greg-whiteboard.herokuapp.com/boards/nvdzj' />
            </div>
            <div id='video'>
                <script>
                    console.log("start");
                    //navigator.getUserMedia  = navigator.mediaDevices.getUserMedia || navigator.getUserMedia || navigator.webkitGetUserMedia || navigator.mozGetUserMedia;
            
                    console.log(navigator.getUserMedia);
                    //var constraints = {audio:true, video:true};
            
                    var facingMode = "user";
                    var constraints = {
                        audio: false,
                        video: {
                            mandatory: {
                                minWidth: 1280,
                                maxWidth: 1280,
                                maxHeight: 720,
                                minHeight: 720
                            }
                        }
                    }
            
                    var videoArea = document.querySelector("video");
            
            
                    var video = document.createElement('video');
            
                    video.style.width = document.width + 'px';
                    video.style.height = document.height + 'px';
                    video.setAttribute('autoplay', '');
                    video.setAttribute('muted', '');
                    video.setAttribute('playsinline', '');
            
            
                    navigator.mediaDevices.getUserMedia(constraints).then(function success(stream) {
                        video.srcObject = stream;
                        document.body.appendChild(video);
                    });
            
                    //var res = navigator.mediaDevices.getUserMedia(constraints, onSuccess, onError);
                </script>
            </div>
            <div id='documents'>Here are the docs</div>
        </div>
    </main>

</body>

</html>